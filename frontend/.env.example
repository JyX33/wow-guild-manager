# API Configuration
# Backend API URL for production builds (e.g., https://api.yourdomain.com)
# For development, this is often set to /api to utilize the Vite proxy defined in vite.config.ts
VITE_API_URL=

# Vite Dev Server Proxy Target
# URL of the backend API for the Vite development proxy (used when VITE_API_URL is NOT set)
VITE_DEV_PROXY_TARGET=http://localhost:5000

# Debug Settings
VITE_DEBUG_MODE=false

# Feature Flags
VITE_ENABLE_FEATURE_X=false